# Manufacturing Agent System — Python Dependencies
# pip install -r requirements.txt

# ── LLM (direct SDK — no LiteLLM proxy needed) ───────────────
groq>=0.9.0

# ── CrewAI core only ─────────────────────────────────────────
# Do NOT install litellm[proxy] — it pulls in fastapi and causes
# ImportError on minimal environments. crewai's core depends on
# litellm base, which is fine; only the [proxy] extra breaks things.
crewai>=0.80.0

# ── Web scraping ──────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── Free search (no API key needed) ──────────────────────────
duckduckgo-search>=6.1.0

# ── Server ───────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0
